def stability(df_rec, df_pay, Lookback_steps):
    value = 0

    assert 0.0 <= value <= 1.0, 'Stability measurement not in range'
    return value


def heterogeneity(df_rec, df_pay, Lookback_steps):
    value = 0


    assert 0.0 <= value <= 1.0, 'Heterogeneity measurement not in range'
    return value
